import { PeliculaImagenPipe } from './pelicula-imagen.pipe';

describe('PeliculaImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
